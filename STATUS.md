# Release status

Current status: **Candidate — tutorial source complete, model-backed pre-flight executed, awaiting supported-runtime execution**. The pipeline package (zero-shot video classification plus the adaptation contract for a closed label set: the HMDB51 clip sample, top-1 / top-3 accuracy with macro recall and F1, bounded fine-tuning of the fusion head, the verified adapter), offline unit tests (including the adaptation, role-helper, import-boundary and notebook-parity suites), the model-backed suite, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/xclip_video_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR4). The earlier `TASK-INFERENCE` notebook's Kaggle CPU run (2026-09-14) is retained as history and is not evidence for the `E2E` blob; the repository is not release-grade until a Colab/Kaggle execution of the exact candidate notebook blob is recorded in `docs/release-verification.md`.
